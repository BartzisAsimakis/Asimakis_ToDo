<Window x:Class="AI_ex_8.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Λίστα Εργασιών" Height="550" Width="900"
        WindowStartupLocation="CenterScreen"
        Background="#f5f7fa">

    <Window.Resources>
        <!-- Στυλ για στρογγυλεμένα κουμπιά -->
        <Style TargetType="Button">
            <Setter Property="Background" Value="#2196F3"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="10,6"/>
            <Setter Property="Margin" Value="0,0,12,0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="8"
                                SnapsToDevicePixels="True">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"
                                              RecognizesAccessKey="True"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid Margin="15" Background="White" SnapsToDevicePixels="True">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Κουμπιά ελέγχου -->
        <StackPanel Orientation="Horizontal" Grid.Row="0" Margin="0,0,0,12">
            <Button x:Name="addButton" Content="➕ Προσθήκη"
                    Width="140" Height="40"
                    Background="#4CAF50"
                    Click="addButton_Click"/>
            <Button x:Name="completeButton" Content="✔ Ολοκληρώθηκε"
                    Width="160" Height="40"
                    Background="#2196F3"
                    Click="completeButton_Click"/>
            <Button x:Name="removeButton" Content="🗑 Κατάργηση"
                    Width="140" Height="40"
                    Background="#FF5722"
                    Click="removeButton_Click"/>
            <Button x:Name="editButton" Content="Επεξεργασία"
        Width="140" Height="40"
        Background="BlueViolet"
        Click="editButton_Click"/>
            <Button x:Name="exitButton" Content="❌ Έξοδος"
                    Width="120" Height="40"
                    Background="#9E9E9E"
                    Click="exitButton_Click"/>
        </StackPanel>

        <!-- Λίστα εργασιών -->
        <Border Grid.Row="1" BorderBrush="#ddd" BorderThickness="1" CornerRadius="8">
            <ListView x:Name="toDo_ListView" FontSize="15" BorderThickness="0"
                      SelectionMode="Single">
                <ListView.View>
                    <GridView>
                        <GridViewColumn Header="Εργασία" DisplayMemberBinding="{Binding Task}" Width="300"/>
                        <GridViewColumn Header="Ημερομηνία" 
                                        DisplayMemberBinding="{Binding Date, StringFormat={}{0:dd/MM/yyyy}}" Width="160"/>
                        <GridViewColumn Header="Υπενθύμιση" 
                                        DisplayMemberBinding="{Binding NextReminderDisplay}" Width="160"/>
                        <GridViewColumn Header="Ολοκληρωμένη" Width="140">
                            <GridViewColumn.CellTemplate>
                                <DataTemplate>
                                    <CheckBox IsChecked="{Binding Completed}"
                                              IsEnabled="False"
                                              HorizontalAlignment="Center"/>
                                </DataTemplate>
                            </GridViewColumn.CellTemplate>
                        </GridViewColumn>
                    </GridView>
                </ListView.View>
            </ListView>
        </Border>
    </Grid>
</Window>
